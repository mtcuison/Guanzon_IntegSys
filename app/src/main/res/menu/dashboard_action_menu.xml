<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_creditApp_menu"
        android:title="Menu"
        tools:ignore="HardcodedText"
        android:orderInCategory="1"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_menu">

        <menu>

            <item android:id="@+id/action_menu_logout"
                android:icon="@drawable/ic_action_menu_exit"
                android:enabled="true"
                android:title="Logout"
                tools:targetApi="o"
                tools:ignore="HardcodedText" />

            <item android:id="@+id/action_menu_exportDb"
                android:icon="@drawable/ic_file_export"
                android:enabled="true"
                android:title="Export Local"
                tools:targetApi="o"
                tools:ignore="HardcodedText" />

        </menu>

    </item>

</menu>